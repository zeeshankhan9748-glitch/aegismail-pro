import { PlaceholderPage } from '@/components/placeholder-page';

export default function InboxCheckerPage() {
  return <PlaceholderPage title="Inbox Checker" description="Planned only for operator-owned seed accounts. No inbox testing implementation ships in Phase 1." />;
}
