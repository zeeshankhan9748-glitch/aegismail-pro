import { PlaceholderPage } from '@/components/placeholder-page';

export default function CampaignsPage() {
  return <PlaceholderPage title="Campaigns" description="Campaign scheduling, launch controls, and recipient lifecycle arrive in a later phase." />;
}
